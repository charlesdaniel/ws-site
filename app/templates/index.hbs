<h1>WS-RS <small>{{t 'home.tag'}}</small></h1>
<hr>
<section>
  <h3>{{t 'home.about.header'}}</h3>
  <p>{{t 'home.about.content'}}
  {{link-to (t 'home.about.apiLink') 'docs'}}
  {{t 'home.about.afterApiLink'}}
  {{link-to (t 'home.about.deflateLink') 'guide.deflate'}}
  {{t 'home.about.afterDeflateLink'}}
  {{link-to (t 'home.about.sslLink') 'guide.ssl'}}{{t 'home.about.afterSslLink'}}
  {{link-to (t 'home.about.testLink') 'testing'}}
  {{t 'home.about.afterTestLink'}}
  {{link-to (t 'home.about.examplesLink') 'examples'}}
  {{t 'home.about.afterExamplesLink'}}
  {{link-to (t 'home.about.guideLink') 'guide'}}
  {{t 'home.about.afterGuideLink'}}</p>
</section>

<section>
  <h3>{{t 'home.benchmark.header'}}</h3>

  <p>{{t 'home.benchmark.intro'}}</p>

  <p>{{t 'home.benchmark.value'}}</p>

  <table class="table table-striped table-condensed">
    <thead>
      <tr>
        <th>{{t 'home.benchmark.library'}}</th>
        <th>{{t 'home.benchmark.time'}}</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{t 'home.benchmark.wsrs.link'}}</td>
        <td>{{t 'home.benchmark.wsrs.time'}}</td>
      </tr>
      <tr>
        <td>{{t 'home.benchmark.libwebsockets.link'}}</td>
        <td>{{t 'home.benchmark.libwebsockets.time'}}</td>
      </tr>
      <tr>
        <td>{{t 'home.benchmark.rustwebsocket.link'}}</td>
        <td>{{t 'home.benchmark.rustwebsocket.time'}}</td>
      </tr>
      <tr>
        <td>{{t 'home.benchmark.websockets.link'}}</td>
        <td>{{t 'home.benchmark.websockets.time'}}</td>
      </tr>
      <tr>
        <td>{{t 'home.benchmark.autobahn.link'}}</td>
        <td>{{t 'home.benchmark.autobahn.time'}}</td>
      </tr>
      <tr>
        <td>{{t 'home.benchmark.node.link'}}</td>
        <td>{{t 'home.benchmark.node.time'}}</td>
      </tr>
    </tbody>
    </table>

    <p>{{t 'home.benchmark.websockets.events'}}</p>
    <p>{{t 'home.benchmark.node.events'}}</p>

    <p>{{t 'home.benchmark.disclaimer'}}</p>
</section>

